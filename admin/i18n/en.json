{"⚠️ First start: the first administrator is created when the instance starts for the first time. If the password field stays empty, the generated password is written ONCE into the ioBroker log (open “Logs” on the left, look for the yellow line with “created with the start password”) — both fields are in the tab “Migration and backup”. The app asks for a new password at the first login.":"⚠️ First start: the first administrator is created when the instance starts for the first time. If the password field stays empty, the generated password is written ONCE into the ioBroker log (open “Logs” on the left, look for the yellow line with “created with the start password”) — both fields are in the tab “Migration and backup”. The app asks for a new password at the first login.","⚠️ The account of the first administrator is created at the very first start. If the password field below stays empty, the adapter generates a random password and writes it ONCE into the ioBroker log: open “Logs” in the ioBroker admin on the left and look for the yellow line with “created with the start password” — that is the password for the first login. The app then asks for a new password right away. Enter your own password below instead if you want to skip that.":"⚠️ The account of the first administrator is created at the very first start. If the password field below stays empty, the adapter generates a random password and writes it ONCE into the ioBroker log: open “Logs” in the ioBroker admin on the left and look for the yellow line with “created with the start password” — that is the password for the first login. The app then asks for a new password right away. Enter your own password below instead if you want to skip that.","127.0.0.1 = only this machine":"127.0.0.1 = only this machine","Badge link secret (HMAC)":"Badge link secret (HMAC)","Bind address":"Bind address","Calculate absences only until today":"Calculate absences only until today","Database file (empty = adapter data directory)":"Database file (empty = adapter data directory)","Days the offline queue accepts on its own":"Days the offline queue accepts on its own","Default language for new users":"Default language for new users","empty = a random password is written to the adapter log once; it has to be changed at the first login":"empty = a random password is written to the adapter log once; it has to be changed at the first login","empty = the adapter generates one once and stores it next to the database, so sessions survive restarts":"empty = the adapter generates one once and stores it next to the database, so sessions survive restarts","Enable kiosk terminal":"Enable kiosk terminal","First start: the start password is in the ioBroker log":"First start: the start password is in the ioBroker log","General":"General","Holiday country":"Holiday country","Instance time zone":"Instance time zone","Keep database backups for days":"Keep database backups for days","Login of the first administrator":"Login of the first administrator","Migration and backup":"Migration and backup","only switch this on when a reverse proxy is in front of the adapter":"only switch this on when a reverse proxy is in front of the adapter","Port":"Port","Round quick punches to minutes (0 = off)":"Round quick punches to minutes (0 = off)","Security":"Security","Session lifetime in minutes":"Session lifetime in minutes","Session secret":"Session secret","Start password of the first administrator":"Start password of the first administrator","Subtract working time from absences":"Subtract working time from absences","the port of the web app, the API and the terminal; 8082 stays free for vis and web":"the port of the web app, the API and the terminal; 8082 stays free for vis and web","Time rules":"Time rules","Trust the reverse proxy (X-Forwarded-*)":"Trust the reverse proxy (X-Forwarded-*)","used once: the account is created when the instance has no administrator yet":"used once: the account is created when the instance has no administrator yet","time-tracker adapter settings":"Adapter settings for time-tracker"}
